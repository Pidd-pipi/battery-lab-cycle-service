const items=document.querySelector('#items'),message=document.querySelector('#message');
async function load(){const r=await fetch('/api/cells');const cells=await r.json();items.innerHTML=cells.map(c=>`<tr><td>${c.id}</td><td>${c.chemistry}</td><td>${c.cycle}</td><td>${c.capacity_mah} mAh</td><td>${c.voltage_v} V</td><td>${c.status}</td><td><button data-id="${c.id}">完成</button></td></tr>`).join('');items.querySelectorAll('button').forEach(b=>b.onclick=async()=>{const r=await fetch('/api/cells/status',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:b.dataset.id,status:'complete'})});message.textContent=r.ok?'循环已标记完成':'更新失败';load()})}load();
